# Expense Tracker

Web App per l’esame di Sviluppo di Applicazioni Web.

## Stack
- React + Vite + TypeScript
- Firebase (Auth + Firestore) — step successivi
- PWA (offline fallback + push) — step successivi

## Dev
```bash
npm install
npm run dev
